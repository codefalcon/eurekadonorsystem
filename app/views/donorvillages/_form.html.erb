<%= form_for(@donorvillage) do |f| %>
  <% if @donorvillage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@donorvillage.errors.count, "error") %> prohibited this donorvillage from being saved:</h2>

      <ul>
      <% @donorvillage.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :village %><br>
    <%= f.collection_select :village_id, @villages, :id, :name, :include_blank => "Select a village" %>
  </div>
  <div class="field">
    <%= f.label :user %><br>
    <%= f.collection_select :user_id, @donors, :id, :name, :include_blank => "Select a donor" %>
  </div>
  <div class="field">
    <%= f.label :start_date %><br>
    <%= f.date_field :start_date %>
  </div>
  <div class="field">
    <%= f.label :end_date %><br>
    <%= f.date_field :end_date %>
  </div>
  <div class="field">
    <%= f.label :amount_for_village %><br>
    <%= f.text_field :amount_for_village %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.number_field :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
